/** Stay under Supabase Free global limit (50 MB). */
export const SAFE_STORAGE_UPLOAD_BYTES = 45 * 1024 * 1024;

/** App-side selection cap before compression. */
export const MAX_VIDEO_BYTES = 200 * 1024 * 1024;
